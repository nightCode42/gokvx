# gitleaks configuration — https://github.com/gitleaks/gitleaks
# Extends the built-in rule set; add narrowly scoped allowlist entries only.

title = "gokvx"

[extend]
useDefault = true

[allowlist]
description = "Files that cannot contain real secrets"
paths = [
  '''go\.sum$''',
]
